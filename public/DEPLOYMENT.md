# 🚀 快速部署指南

## ⚠️ 重要提醒

如果您遇到了 SQL 语法错误，请使用 `supabase-schema-fixed.sql` 文件替代原来的 `supabase-schema.sql`。

## 📋 部署步骤

### 第一步：设置 Supabase

1. **创建 Supabase 项目**
   - 访问 [supabase.com](https://supabase.com)
   - 点击 "Start your project"
   - 选择 GitHub/Google 登录
   - 创建新项目，选择离你最近的地区

2. **执行数据库架构**
   - 在 Supabase 项目中，点击左侧菜单的 "SQL Editor"
   - 复制 `supabase-schema-fixed.sql` 文件的内容
   - 粘贴到 SQL 编辑器中
   - 点击 "Run" 执行

3. **获取配置信息**
   - 进入项目设置 (Settings → API)
   - 复制以下信息：
     - Project URL (类似: `https://xxx.supabase.co`)
     - anon public key (类似: `eyJhbGciOiJIUzI1NiIs...`)

### 第二步：配置项目

1. **更新配置文件**
   - 打开 `js/config.js`
   - 替换以下内容：
     ```javascript
     SUPABASE_URL: 'https://your-project-id.supabase.co', // 替换为你的 URL
     SUPABASE_ANON_KEY: 'your-supabase-anon-key-here', // 替换为你的 anon key
     ```

2. **测试本地运行**
   - 直接用浏览器打开 `index.html`
   - 检查浏览器控制台是否有错误
   - 确认数据加载正常

### 第三步：部署到 Netlify

1. **方法一：拖拽部署（最快）**
   - 将整个项目文件夹压缩成 ZIP
   - 访问 [netlify.com](https://netlify.com)
   - 将 ZIP 文件拖拽到页面上的部署区域
   - 等待部署完成

2. **方法二：Git 集成（推荐）**
   - 将代码上传到 GitHub/GitLab
   - 在 Netlify 中连接仓库
   - 设置：
     - Build command: `echo "No build command needed"`
     - Publish directory: `.`
   - 点击 "Deploy site"

3. **设置环境变量（可选但推荐）**
   - 在 Netlify 项目设置中找到 "Environment variables"
   - 添加：
     - Key: `SUPABASE_URL`, Value: `你的 Supabase URL`
     - Key: `SUPABASE_ANON_KEY`, Value: `你的 Supabase 匿名密钥`
   - 重新部署网站

### 第四步：验证部署

1. **基本功能测试**
   - 浏览产品列表
   - 测试添加到购物车
   - 测试用户注册/登录

2. **配置认证重定向**
   - 回到 Supabase 项目
   - 进入 Authentication → Settings
   - 在 "Site URL" 中填入：`https://你的网站域名.netlify.app`
   - 在 "Redirect URLs" 中添加：`https://你的网站域名.netlify.app/**`

## 🔧 常见问题

### Q: SQL 执行失败怎么办？
A: 使用 `supabase-schema-fixed.sql` 文件，它修复了语法错误。

### Q: 网站加载很慢？
A: 检查图片 URL 是否有效，考虑使用 CDN。

### Q: 用户注册失败？
A: 检查 Supabase 认证设置，确保重定向 URL 正确。

### Q: 购物车数据不保存？
A: 检查用户是否正确登录，确认 Supabase 连接正常。

### Q: 如何添加更多产品？
A: 在 Supabase 的 Table Editor 中直接编辑 `products` 表。

## 📞 需要帮助？

1. 检查浏览器控制台错误信息
2. 查看 Netlify 构建日志
3. 检查 Supabase 项目日志
4. 参考 `README.md` 文件获取更多信息

---

**快速提示**: 如果只是想快速体验，可以直接使用本地存储模式（修改 `js/config.js` 中的 Supabase 配置为空即可）。